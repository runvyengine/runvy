-- THIS FILE IS GENERATED. DO NOT EDIT BY HAND.
-- The engine layer (`runvy_base.luau`, built-in types) is regenerated by the
-- `gen-runvy-base` binary; your project's own types are appended from `inventory`.
-- Read by `luau-lsp` for type checking and autocomplete.

export type Vec3 = { x: number, y: number, z: number }
export type Vec4 = { x: number, y: number, z: number, w: number }
export type Vec2 = { x: number, y: number }
export type Quat = { x: number, y: number, z: number, w: number }

export type KeyCode = "KeyA" | "KeyB" | "KeyC" | "KeyD" | "KeyE" | "KeyF" | "KeyG" | "KeyH" | "KeyI" | "KeyJ" | "KeyK" | "KeyL" | "KeyM" | "KeyN" | "KeyO" | "KeyP" | "KeyQ" | "KeyR" | "KeyS" | "KeyT" | "KeyU" | "KeyV" | "KeyW" | "KeyX" | "KeyY" | "KeyZ" | "Digit0" | "Digit1" | "Digit2" | "Digit3" | "Digit4" | "Digit5" | "Digit6" | "Digit7" | "Digit8" | "Digit9" | "ArrowUp" | "ArrowDown" | "ArrowLeft" | "ArrowRight" | "Space" | "Enter" | "Escape" | "Tab" | "Backspace" | "Delete" | "Home" | "End" | "PageUp" | "PageDown" | "Insert" | "ShiftLeft" | "ShiftRight" | "ControlLeft" | "ControlRight" | "AltLeft" | "AltRight" | "Meta" | "SuperLeft" | "SuperRight" | "CapsLock" | "F1" | "F2" | "F3" | "F4" | "F5" | "F6" | "F7" | "F8" | "F9" | "F10" | "F11" | "F12" | "Backquote" | "Minus" | "Equal" | "BracketLeft" | "BracketRight" | "Backslash" | "Semicolon" | "Quote" | "Comma" | "Period" | "Slash" | "Numpad0" | "Numpad1" | "Numpad2" | "Numpad3" | "Numpad4" | "Numpad5" | "Numpad6" | "Numpad7" | "Numpad8" | "Numpad9" | "NumpadAdd" | "NumpadSubtract" | "NumpadMultiply" | "NumpadDivide" | "NumpadDecimal" | "NumpadEnter"

export type ScriptInput = {
    KeyA: boolean,
    KeyB: boolean,
    KeyC: boolean,
    KeyD: boolean,
    KeyE: boolean,
    KeyF: boolean,
    KeyG: boolean,
    KeyH: boolean,
    KeyI: boolean,
    KeyJ: boolean,
    KeyK: boolean,
    KeyL: boolean,
    KeyM: boolean,
    KeyN: boolean,
    KeyO: boolean,
    KeyP: boolean,
    KeyQ: boolean,
    KeyR: boolean,
    KeyS: boolean,
    KeyT: boolean,
    KeyU: boolean,
    KeyV: boolean,
    KeyW: boolean,
    KeyX: boolean,
    KeyY: boolean,
    KeyZ: boolean,
    Digit0: boolean,
    Digit1: boolean,
    Digit2: boolean,
    Digit3: boolean,
    Digit4: boolean,
    Digit5: boolean,
    Digit6: boolean,
    Digit7: boolean,
    Digit8: boolean,
    Digit9: boolean,
    ArrowUp: boolean,
    ArrowDown: boolean,
    ArrowLeft: boolean,
    ArrowRight: boolean,
    Space: boolean,
    Enter: boolean,
    Escape: boolean,
    Tab: boolean,
    Backspace: boolean,
    Delete: boolean,
    Home: boolean,
    End: boolean,
    PageUp: boolean,
    PageDown: boolean,
    Insert: boolean,
    ShiftLeft: boolean,
    ShiftRight: boolean,
    ControlLeft: boolean,
    ControlRight: boolean,
    AltLeft: boolean,
    AltRight: boolean,
    Meta: boolean,
    SuperLeft: boolean,
    SuperRight: boolean,
    CapsLock: boolean,
    F1: boolean,
    F2: boolean,
    F3: boolean,
    F4: boolean,
    F5: boolean,
    F6: boolean,
    F7: boolean,
    F8: boolean,
    F9: boolean,
    F10: boolean,
    F11: boolean,
    F12: boolean,
    Backquote: boolean,
    Minus: boolean,
    Equal: boolean,
    BracketLeft: boolean,
    BracketRight: boolean,
    Backslash: boolean,
    Semicolon: boolean,
    Quote: boolean,
    Comma: boolean,
    Period: boolean,
    Slash: boolean,
    Numpad0: boolean,
    Numpad1: boolean,
    Numpad2: boolean,
    Numpad3: boolean,
    Numpad4: boolean,
    Numpad5: boolean,
    Numpad6: boolean,
    Numpad7: boolean,
    Numpad8: boolean,
    Numpad9: boolean,
    NumpadAdd: boolean,
    NumpadSubtract: boolean,
    NumpadMultiply: boolean,
    NumpadDivide: boolean,
    NumpadDecimal: boolean,
    NumpadEnter: boolean,
    keys_just_pressed: { [string]: boolean },
    mouse_buttons_pressed: { [string]: boolean },
    mouse_buttons_just_pressed: { [string]: boolean },
    mouse_position: { x: number, y: number },
    mouse_delta: { x: number, y: number },
    mouse_wheel_delta: number,
    is_key_pressed: (self: ScriptInput, key: KeyCode) -> boolean,
    is_key_just_pressed: (self: ScriptInput, key: KeyCode) -> boolean,
    is_mouse_pressed: (self: ScriptInput, button: string) -> boolean,
    is_mouse_just_pressed: (self: ScriptInput, button: string) -> boolean,
    get_mouse_position: (self: ScriptInput) -> { x: number, y: number },
}

export type ScriptEvent = { name: string, x: number, y: number }

--- 2D collider shape: a rect (half-size) or a circle.
export type Collider2DShape = { type: "Rect", half_size: Vec2 } | { type: "Circle", radius: number }

--- 3D collider shape: a box (half-size) or a sphere.
export type Collider3DShape = { type: "Box", half_size: Vec3 } | { type: "Sphere", radius: number }

--- Camera projection mode.
export type ProjectionType = "Orthographic" | "Perspective"

--- Screen-effect toggles; each field enables the matching effect.
export type ScreenEffectFlags = { fade: boolean, vignette: boolean, rgb_shift: boolean, tint: boolean, color_adjust: boolean }

--- Active camera marker component
--- Attach this to an object to make it the active camera.
--- The object should also have either Camera2D or Camera3D component.
--- # Example
--- ```rust,ignore
--- // In your camera script's construct:
--- object.add_component(Camera3D { ... });
--- object.add_component(ActiveCamera);
--- ```
export type ActiveCamera = {
}
export type AudioListener = {
    --- Listener volume (0.0 to 1.0)
    volume: number,
    --- Is this listener active (only one active listener at a time)
    active: boolean,
    --- Stereo separation factor (0.0 = mono, 1.0 = full stereo panning)
    --- Controls how much sounds pan between left and right channels based on
    --- their position relative to the listener. Higher values mean more
    --- extreme panning.
    stereo_separation: number,
}
--- Audio source component — attaches audio to a game object
export type AudioSource = {
    --- Relative asset path used to reload this source in editor/runtime
    source_path: string,
    --- Playback volume (0.0 to 1.0)
    volume: number,
    --- Loop playback
    looped: boolean,
    --- Is currently playing
    playing: boolean,
    --- Play automatically when object spawns
    play_on_awake: boolean,
    --- Request playback on next world update
    play_requested: boolean,
    --- Request stop on next world update
    stop_requested: boolean,
    --- Minimum distance for sound attenuation (3D sound)
    min_distance: number,
    --- Maximum distance for sound attenuation (3D sound)
    max_distance: number,
    --- Is this a 3D sound (affected by position and listener)
    spatial: boolean,
}
--- A shared camera component with 2D and 3D support.
export type Camera = {
    --- Camera position in world space.
    position: Vec3,
    --- Look target for 3D cameras.
    target: Vec3,
    --- Up direction for 3D cameras.
    up: Vec3,
    --- Projection mode.
    projection: ProjectionType,
    --- Orthographic camera size as width and height.
    orthographic_size: Vec2,
    --- Near clipping plane.
    near: number,
    --- Far clipping plane.
    far: number,
    --- Field of view in radians for 3D cameras.
    fov: number,
}
--- 2D collider attached to an entity. The world-space position/orientation
--- comes from `Transform`; this struct only holds the local shape + flags.
export type Collider2D = {
    shape: Collider2DShape,
    offset: Vec2,
    enabled: boolean,
    is_trigger: boolean,
    layer: number,
}
--- 3D collider attached to an entity. World position/orientation comes from
--- `Transform`; this only holds the local shape + flags.
export type Collider3D = {
    shape: Collider3DShape,
    offset: Vec3,
    enabled: boolean,
    is_trigger: boolean,
    layer: number,
}
--- RGBA color with f32 components in [0..1] range.
--- # Examples
--- ```
--- use runvy_core::Color;
--- let red = Color::rgb(1.0, 0.0, 0.0);
--- let teal = Color::hex("#008080").unwrap();
--- let (h, s, v) = Color::from_hsv(120.0, 0.5, 0.8).to_hsv();
--- ```
export type Color = {
    r: number,
    g: number,
    b: number,
    a: number,
}
export type CursorInteractable = {
    is_pressed: boolean,
    is_hovered: boolean,
    was_hovered: boolean,
    bounds_size: Vec3,
}
export type DirectionalLight = {
    direction: Vec3,
    color: Color,
    intensity: number,
}
export type MeshRenderer = {
    mesh_path: string,
    visible: boolean,
    cast_shadows: boolean,
    receive_shadows: boolean,
    --- Legacy tint kept for compatibility with existing project serialization/editor code.
    color: { [number]: number },
}
export type ObjectDefinitionInstance = {
    object_id: string,
}
export type PhysicsCollision = {
    size: Vec2,
    enabled: boolean,
}
export type PointLight = {
    color: Color,
    intensity: number,
    radius: number,
    falloff: number,
}
export type ScreenEffects = {
    enabled: ScreenEffectFlags,
    fade_color: { [number]: number },
    vignette_strength: number,
    vignette_radius: number,
    vignette_softness: number,
    rgb_shift: { [number]: number },
    tint_color: { [number]: number },
    brightness: number,
    contrast: number,
}
export type Sorting = {
    order: number,
    y_sort: boolean,
    y_offset: number,
}
export type SpriteAnimationClip = {
    name: string,
    start_frame: number,
    end_frame: number,
    fps: number,
    looping: boolean,
}
export type SpriteAnimator = {
    sheet: SpriteSheet,
    clips: { [number]: SpriteAnimationClip },
    current_clip: string,
    current_frame: number,
    playing: boolean,
}
--- 2D sprite component — draws a textured quad.
export type SpriteRenderer = {
    texture_path: string,
    pixels_per_unit: number,
    uv_rect: { [number]: number },
    color: { [number]: number },
    --- When true, output `color` directly (using texture alpha for transparency).
    --- When false, multiply texture with `color` (legacy behavior).
    replace_color: boolean,
    --- Flip the sprite horizontally when rendering.
    flip_x: boolean,
    --- Flip the sprite vertically when rendering.
    flip_y: boolean,
}
export type SpriteSheet = {
    columns: number,
    rows: number,
}
--- Tilemap data component.
export type Tilemap = {
    --- Map size in tiles.
    width: number,
    height: number,
    --- Layers ordered from back to front. Painting expands the map
    --- automatically, growing `width`/`height`/`offset` as needed.
    layers: { [number]: TilemapLayer },
    selected_tile: number,
    pixels_per_unit: number,
}
--- A single tilemap layer: a dense, rectangular grid of [`TileId`]s.
export type TilemapLayer = {
    name: string,
    width: number,
    height: number,
    --- `width * height` elements, row-major. `EMPTY_TILE` (0) is empty.
    tiles: { [number]: TileId },
    visible: boolean,
    opacity: number,
    --- Draw order relative to other layers and world objects.
    order: number,
}
--- Rendering Component for Tilemap.
export type TilemapRenderer = {
}
export type Transform = {
    position: Vec3,
    rotation: Quat,
    scale: Vec3,
}
--- Rectangle used for UV coordinates inside an atlas.
export type UvRect = {
    x: number,
    y: number,
    width: number,
    height: number,
}
export type WorldAtmosphere = {
    ambient_color: Color,
    ambient_intensity: number,
    background_intensity: number,
}

local ActiveCamera: ActiveCamera = {} :: ActiveCamera;
local AudioListener: AudioListener = {} :: AudioListener;
local AudioSource: AudioSource = {} :: AudioSource;
local Camera: Camera = {} :: Camera;
local Collider2D: Collider2D = {} :: Collider2D;
local Collider3D: Collider3D = {} :: Collider3D;
local Color: Color = {} :: Color;
local CursorInteractable: CursorInteractable = {} :: CursorInteractable;
local DirectionalLight: DirectionalLight = {} :: DirectionalLight;
local MeshRenderer: MeshRenderer = {} :: MeshRenderer;
local ObjectDefinitionInstance: ObjectDefinitionInstance = {} :: ObjectDefinitionInstance;
local PhysicsCollision: PhysicsCollision = {} :: PhysicsCollision;
local PointLight: PointLight = {} :: PointLight;
local ScreenEffects: ScreenEffects = {} :: ScreenEffects;
local Sorting: Sorting = {} :: Sorting;
local SpriteAnimationClip: SpriteAnimationClip = {} :: SpriteAnimationClip;
local SpriteAnimator: SpriteAnimator = {} :: SpriteAnimator;
local SpriteRenderer: SpriteRenderer = {} :: SpriteRenderer;
local SpriteSheet: SpriteSheet = {} :: SpriteSheet;
local Tilemap: Tilemap = {} :: Tilemap;
local TilemapLayer: TilemapLayer = {} :: TilemapLayer;
local TilemapRenderer: TilemapRenderer = {} :: TilemapRenderer;
local Transform: Transform = {} :: Transform;
local UvRect: UvRect = {} :: UvRect;
local WorldAtmosphere: WorldAtmosphere = {} :: WorldAtmosphere;
export type ScriptContext = {
    entity: number,
    dt: number,
    input: ScriptInput,
    components: { [string]: any },
    events: { ScriptEvent },
    events_in: { ScriptEvent },
    GetComponent: <T>(self: ScriptContext, component: T) -> T,
    HasComponent: <T>(self: ScriptContext, component: T) -> boolean,
    AddComponent: <T>(self: ScriptContext, component: T, value: any) -> (),
    RemoveComponent: <T>(self: ScriptContext, component: T) -> (),
    Spawn: (self: ScriptContext, path: string) -> (),
    Destroy: (self: ScriptContext, entity: number) -> (),
}

return {
    ActiveCamera = ActiveCamera,
    AudioListener = AudioListener,
    AudioSource = AudioSource,
    Camera = Camera,
    Collider2D = Collider2D,
    Collider3D = Collider3D,
    Color = Color,
    CursorInteractable = CursorInteractable,
    DirectionalLight = DirectionalLight,
    MeshRenderer = MeshRenderer,
    ObjectDefinitionInstance = ObjectDefinitionInstance,
    PhysicsCollision = PhysicsCollision,
    PointLight = PointLight,
    ScreenEffects = ScreenEffects,
    Sorting = Sorting,
    SpriteAnimationClip = SpriteAnimationClip,
    SpriteAnimator = SpriteAnimator,
    SpriteRenderer = SpriteRenderer,
    SpriteSheet = SpriteSheet,
    Tilemap = Tilemap,
    TilemapLayer = TilemapLayer,
    TilemapRenderer = TilemapRenderer,
    Transform = Transform,
    UvRect = UvRect,
    WorldAtmosphere = WorldAtmosphere,
    vec2 = function(x: number, y: number): Vec2 return { x = x, y = y } end,
    vec3 = function(x: number, y: number, z: number): Vec3 return { x = x, y = y, z = z } end,
    vec4 = function(x: number, y: number, z: number, w: number): Vec4 return { x = x, y = y, z = z, w = w } end,
    quat = function(x: number, y: number, z: number, w: number): Quat return { x = x, y = y, z = z, w = w } end,
    normalize2 = function(v: Vec2): Vec2 return v end,
    normalize3 = function(v: Vec3): Vec3 return v end,
    length2 = function(v: Vec2): number return 0 end,
    length3 = function(v: Vec3): number return 0 end,
    pi = 3.141592653589793,
    cos = function(x: number): number return 0 end,
    sin = function(x: number): number return 0 end,
    tan = function(x: number): number return 0 end,
    atan2 = function(y: number, x: number): number return 0 end,
    sqrt = function(x: number): number return 0 end,
    abs = function(x: number): number return 0 end,
    floor = function(x: number): number return 0 end,
    ceil = function(x: number): number return 0 end,
    round = function(x: number): number return 0 end,
    sign = function(x: number): number return 0 end,
    pow = function(x: number, y: number): number return 0 end,
    max = function(a: number, b: number): number return 0 end,
    min = function(a: number, b: number): number return 0 end,
    clamp = function(x: number, lo: number, hi: number): number return 0 end,
    rad = function(x: number): number return 0 end,
    deg = function(x: number): number return 0 end,
}
